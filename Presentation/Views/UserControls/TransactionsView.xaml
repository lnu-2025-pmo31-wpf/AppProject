<UserControl x:Class="Presentation.Views.UserControls.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F4F6FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Transactions" FontSize="26" FontWeight="SemiBold" Margin="0 0 0 10"/>

        <!-- Add form -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="14" Margin="0 0 0 12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Amount" Foreground="#444"/>
                    <TextBox x:Name="AmountBox" Height="32" Margin="0 6 10 0"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Category" Foreground="#444"/>
                    <ComboBox x:Name="CategoryBox" Height="32" Margin="0 6 10 0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Type" Foreground="#444"/>
                    <ComboBox x:Name="TypeBox" Height="32" Margin="0 6 10 0">
                        <ComboBoxItem Content="Expense"/>
                        <ComboBoxItem Content="Income"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="Note" Foreground="#444"/>
                    <TextBox x:Name="NoteBox" Height="32" Margin="0 6 10 0"/>
                </StackPanel>

                <Button Grid.Column="4" Content="Add" Height="34" Margin="0 22 0 0"
                        Click="Add_Click"/>
            </Grid>
        </Border>

        <!-- List -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="10">
            <DockPanel>
                <Button Content="Delete selected" DockPanel.Dock="Top" Height="32" Margin="0 0 0 8"
                        Click="Delete_Click"/>
                <DataGrid x:Name="GridTx"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="120"/>
                        <DataGridTextColumn Header="CategoryId" Binding="{Binding CategoryId}" Width="120"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="200"/>
                        <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
